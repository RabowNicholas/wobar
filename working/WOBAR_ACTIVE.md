---
title: Wobar Active Loops
version: 1.0
last_updated: 2026-06-12
last_session: 2026-06-12
status: live
scope: All open project loops. Read this alongside WOBAR_CONTEXT at the start of any working session. Updated by Claude at the end of each session.
dependencies: [[WOBAR_CONTEXT]]
---

# WOBAR ACTIVE LOOPS

Living document. Not locked. Updated at the end of every working session.
When a loop closes, it moves to [[working/WOBAR_CLOSED]].

## Open Loops

LOOP: down_bad_3stack v002 — Act 4 Phase 1 visualizer for Down Bad remix (Dreamville / J Cole)
STATUS: in progress — full pipeline wired (video timing, audio reactivity, post chain, export). Brand review passed (Nick happy). Pending: final tuning + canonical save + IG-ready render via Pipeline C (PNG → FFmpeg).
LAST: 2026-05-26 (later) — Major session. Full visualizer end-to-end built on top of v001's composition. **Video timing system:** moviefileinTOP `movie_in` re-wired in `specify` playmode with index expression `max(0, min((me.time.seconds - Videodelay + (Videoskipdur if me.time.seconds >= Videoskipat else 0)) * 23.978347, numImages-1))` — plus a SECOND jump (Videojump2at/Videojump2to) for jumping BACK to an earlier video timestamp for the final chorus. 5 dials on ctrl_master `Video` page expose all timing. Currently set: Videodelay=-4, Videoskipat=82, Videoskipdur=45, Videojump2at=137, Videojump2to=40. Song is 193.72s (down_bad_remix_v2 2.mp3, 320kbps stereo) vs original video 183.62s; verse removed by Nick at video-time 1:30-2:03; chorus loop-back at remix t=2:17. **Cook rate retimed 60→30** (heavy comp pipeline couldn't sustain 60 cook-fps causing me.time.seconds slow-mo — drop to 30 + time.end retimed 11650→5825 frames preserves 194.17s duration). **Audio reactivity infrastructure:** new `base_audio_react` baseCOMP at /project1, per-band CHOP normalization layer (selectCHOP per band → mathCHOP preoff=-p10 + gain=1/(p_hi-p10) → renameCHOP to _n suffix → mergeCHOP → null_out) publishing 8 channels (sub_bass_n, bass_n, mid_n, high_n, energy_n, sub_pressure_n, growl_n, transient_n) normalized p10→0/p90→1 (p95 for transient sparse peaks) from rec_audio stats captured during full-song play-through. **Three audio bindings wired** using `Base + React * pow(clamp(audio_n, 0, 2), Curve)` pattern: (1) MID — `sub_pressure_n → base_psy/level.opacity` (atmospheric body swell, slow-lag feedback decay; Echoreact/Echocurve dials added), (2) TOP — `bass_n → base_color/level_posterize.stepsize` (sustained body weight chunkiness; Posterizereact/Posterizecurve dials added), (3) BOT — beat-pulse driven CA via global chain. **Beat pulse system:** `exp(-((me.time.seconds * BPM / 60 / div) % 1) * decay)` with audio scalar layer; tried as edge brightness flash first (didn't read on binary line art), then bass→Edgethicken (inverted because blur is before edge), then mid→Edgethicken (vocals not punchy enough), then transient (works analytically but sparse → invisible flashes on bottom panel). Final move: beat pulse drives **global CA** via new chain `layout_stack → [ca_keep_red/green/blue split via levelTOPs] → [ca_xform_red +tx / ca_xform_blue -tx via beat pulse] → ca_recombine (add) → null_out`. 4 new beat dials: Beatbpm=140, Beatdiv=1, Beatdecay=6, Beataudio=0.5. **Cinematic post chain:** ca_recombine → `level_grade` (Cineblacks/Cinegamma/Cinecontrast) → `hsv_grade` (Cinesat) → `bloom_post` (Bloomint/Bloomthresh/Bloomradius, inputplusbloom) → `grain_apply` (animated simplex2d noise screen-blended, Grainamount; per IG strategy: grain protects against 4:2:0 banding through re-encode). 8 dials on new ctrl_master `Post` page. **ctrl_master cleaned up + reorganized** into 4 logical pages: Top (color/posterize panel pars) / Mid (psy/feedback pars + Echoreact+Echocurve) / Bot (edge pars + Edgereact+Edgecurve + Caoffset + Beatbpm/Beatdiv/Beatdecay/Beataudio) / Video (timing dials). Plus new Post page. **Export:** moviefileoutTOP `movie_out` wired with audio from `base_audio/audio_in` (NOT the sequential `down_bad_remix_v2_2` — that was the bug causing audio-from-zero in export while preview synced to timeline). Specs: mp4 / h264 / high profile / yuv420 / cabac / Rec.709 / 18Mbps avg, 24 peak / 30fps / AAC 320kbps stereo / length 194.17s / output `touchdesigner/networks/down_bad_3stack/renders/down_bad_3stack.NNN.mp4` (uniquesuff auto-increments). Nick: "I think its looking really good" — visual passes his bar; brand review surfaced 5 minor misses (no forward propulsion, CA might be too aggressive at 8px, palette not verified, no "discharge" visual moment, no "full palette activated" feel) — Nick deferred all as "not a big deal." Resolution still 720×1280 (NC license cap is 1280×1280 anyway).
NEXT: (1) Save canonical `.toe` checkpoint as v002 (network not saved since session start — risk losing today's work). (2) Final IG-ready render via Pipeline C (PNG image sequence → FFmpeg with `-crf 18 -preset slow -tune grain -aq-mode=3` per `WOBAR_TD_REFERENCE.md §8`) — the in-TD H.264 moviefileoutTOP is the convenient one-shot path but Pipeline C gives visibly cleaner output through IG's re-encoder. (3) Resolution bump to 1080×1920 for IG-final (currently 720×1280, NC license caps at 1280×1280 so 720×1280 stays the operating size and Pipeline C can upscale at FFmpeg stage if needed). (4) Optional: address deferred brand misses — add slow continuous palette hue cycle for "forward propulsion", drop Caoffset default to 4 for nostalgic register, verify ramp_palette is WOBAR colors. (5) Optional: audio reactivity binding #4 (energy → Cinecontrast atmospheric global breath).


LOOP: Release Schedule
STATUS: in progress — system LIVE and working. MUR #01 shipped on time; #02 on track.
LAST: 2026-06-10 — loop status verified with Nick after a month stale. **MUR (#01) SHIPPED 2026-05-29 on time, full 7-asset packet done** (4 clips 30–60s + 1 hook 15s + artwork + track) — the v2 Mirror-only packet did its job; the T-21 deadline (5/8) was hit. **Release #02 confirmed on track for 6/12** (T-21 was 5/22 — matches last RELEASE_PACKET.xlsx update commit `573c5ea`). System mechanics (unchanged): 16 releases biweekly Fridays 5/29 → 12/25, 7 assets per release, each visualizer 2× (TK + IG) = 10 posts per 14-day window, BTS as trigger-based companion (Mirror clip clears 1.5× baseline → 1 BTS clip 24–48h after, cap 3/release), T-counter dates auto-calc from B6, TD output floor 3/week. Mirror posts are going out but no BTS trigger has fired yet (none cleared 1.5× baseline as of 6/10). Walls Are Thin review resolved → quiet retirement (loop closed 6/10).
NEXT: (1) Ship #02 Friday 6/12. (2) **#03 (6/26) T-21 was 6/5 — already past.** Verify #03 distributor upload happened; if missed, #03 slides to 7/10 and year-end count drops. (3) Per-release packet build stays the daily job; windows overlap by 7 days (current at T+7 while next at T-7) by design. (4) Watch for first BTS trigger fire — first real data point for the trigger system.


LOOP: POPX v1.4.0 Upgrade
STATUS: in progress — docs updated + install confirmed; hands-on validation pending
LAST: 2026-05-26 — pulled the v1.4.0 changelog from popx.tools/changelog and updated the brain system. `reference/td_library/TD_POPX_GUIDE.md` bumped 1.1 → 1.2 (frontmatter, source path, install state, aim example path, asset-loader gotcha all repathed to `touchdesigner/third_party/POPX/POPX_Examples_1_4_0/`). New `## v1.4.0 Changelog Highlights` section near the top documents: (1) **Filter** modifier (new — 6 algorithms incl. One Euro adaptive smoothing, falloff-attenuated; documented as its own module section between Spring and Texture Falloff); (2) **breaking removal** of per-curve toggles on Sweep / Move Along Curve / Orient Curve (per-curve mapping is now auto from TOP rows); (3) **ramp UI renames** across ~10 modules (Custom Ramp Editor consolidation — `Ramp TOP → Custom * TOP`, `Open … Ramp → Open … Ramp Editor`); (4) **Path Tracer** gains DLSS (Windows/NVIDIA only — M1 stays on SVGF/OptiX), Microfacet Refraction, per-channel OptiX reject thresholds; (5) **POPX Light** gains Diffuse/Specular Contribution; (6) **Move Along Mesh** Distribution menu (already in table); (7) Soft Body / Relax / DLG collision accuracy improved; (8) **Material `Emssion*` typo FIXED → `Emission*`** — fixed inline at lines 490 and 525 of the guide. Two index files also updated: `WOBAR_TD_INDEX.md` (POPX pointer mentions v1.4.0 + the changelog block) and `td_library/TD_LIBRARY_INDEX.md` (POPX row updated to 1.4.0, calls out Filter + Path Tracer DLSS). Historical artifacts (network move files, CHANGE_LOGs, prior ACTIVE rows, TD_BUILD_LOG entries) intentionally not retroactively rewritten — they correctly record what was true under 1.3.0. **Nick confirmed (same date): POPX_1_4_0.tox is installed in the live TD project at `/POPX_1_4_0`; TD build 2025.32460 stays put — build-mismatch warning vs POPX's 2025.32820 target is accepted for now.**
NEXT: (1) First WOBAR-on-1.4.0 build — validate Filter modifier on a real audio-driven attribute. Best candidate is the down_bad_3stack `Echoopacity` binding (Act 4 P1, currently unwired): try One Euro on the bass channel as a cleaner smoother than naive bass mapping. This becomes the proof-of-value for the upgrade. (2) When next opening any prior network that uses Color Modifier ramps, Texture Falloff Remap params, or Sweep/Move Along Curve TOP modulation, do a quick pass to confirm no params were silently renamed under us (Custom Ramp Editor rename pass).


LOOP: crease_veil — Act 3 ENCOUNTER visualizer for crease (latex-veil concept, distinct from crease_sa/sa_1)
STATUS: in progress — **latex read LOCKED** (FELT pole, glove model); mid-build at `/project1/crease_veil` in live `NewProject.1.toe`. Checkpoint `crease_veil_v001.tox` saved. Next: DLA height-source pivot.
LAST: 2026-06-16 — long build session. **Mechanic evolved twice via on-screen testing** (see `working/CREASE_VEIL_BRIEF.md` revisions): (1) literal *pressing* dropped — luminous entity behind thin latex washes to flat hot dots, glow vs 3D-dome are opposite lanes; (2) hybrid swell also failed trapped read ("just move closer" = light-in-depth). **Root insight: SEEN (luminous = window/watching) vs FELT (pressure imprinted in front-lit surface = skin/trapped) read by opposite cues — glow always wins, veil becomes window. Locked FELT pole.** FELT first pass WORKED: front raking light → **sliding specular** = the surface-toward-you cue; continuous lit skin; tension wrinkles; subsurface color; presences blurred. Nick: "feels trapped now." Then nailed **translucent-latex texture**: flipped light source (skin dark/dim, transmission from behind = main light, see-through forms behind), then **GLOVE MODEL** per Nick's ref ("latex glove stretched till you see through it") — opacity drops where stretched → colored forms bloom through the thinned spot + tension-whitening + wet glints. Pushed hard, reads. **Build (2D, glslTOP `membrane`):** blobs→comp_height→displace(noise warp)→blur (dome 42 + presence 90); ambient back-field (noise→blur→level 0.18→comp_presence); latex_grain micro-normal; motion via noise_drift + noise_breath CHOPs on blob center/radius. 3 moves logged + v001.tox saved.
NEXT: (1) **DLA height-source pivot (Nick's direction):** replace blob masses with POPX **DLA** branching tendrils → render top-down to 2D height map → worm/vein "burrowing under skin"; DLA growth = escalation. POPX NOT loaded in this project (fresh NewProject) — load `touchdesigner/third_party/POPX/POPX_1_4_0.tox`; crib `example_projects/DLA.toe`. Keep membrane shader, swap height source only. Watch Mac POPX caveats + perf; checkpoint before sim. (2) Cadence/speed (Nick: "too slow"). (3) Audio reactivity (reference base_audio, don't dup). (4) Post chain (reuse crease_sa family). (5) ctrl_master dials (see-through, skin-dimness, wet-glints, stretch-transparency, color-through already identified). (6) Save as proper crease_veil .toe / decide project home (currently unsaved NewProject on Desktop).


---

## Session Log

| Date | Summary |
|------|---------|
| 2026-06-12 (crease_sa) | **crease_sa (sa_1) — full system session, 18 moves, `sa_1_v001.tox` SAVED, Nick "really liking this." No loop opened per Nick — state lives in `networks/sa_1/CHANGE_LOG.md`.** Act 3 ENCOUNTER Rorschach attractor (mirrored Aizawa, 3000 instanced metallic pearls) taken from raw base to complete treatment in one day. **Brand check first** (new skill's maiden run): 8/10 Act 3 — crease mirror = most literal mirror-encounter realization yet; 4 minor misses logged, one (static interior) closed same-day by ember breath. **Post chain:** threshold bloom (bloomTOP `inputplusbloom` replacing full-frame blur/screen chain), `level_grade`+`hsv_grade` cinematic stage, soft2d shadows ×2 lights (light_back halo revived from orphan at 1.5× dimmer), 1px CA (2px doubled point-source embers — anaglyph read), grain→screen; vignette built then KILLED by Nick. **SACRAMENT color register** born from song theme (psychedelic trip processing religious trauma — "am I dreaming" → "this is reality"): communion wine + tarnished votive gilt + pewter lucidity edge; "warmth must disturb" affinity rule executed literally. **Bar-clock block system** (140 BPM confirmed): WINE→GILT→LUCID→COMMUNION, 8 bars/block, 32-bar meta-cycle — built first as mega-ramp sliding-window lookup (window math verified via `TOP.sample` BEFORE wiring), then MOVED INTO THE LIGHTS per Nick ("color should be the lights"; lookup left dormant at `Colormix=0`), then extended to orb albedo + mat rim after diagnosing **Metallic=1.0 tint-lock** (full metal = no diffuse; all visible color = reflections tinted by static albedo — lights cycled, orbs filtered everything wine). **Instance variation** (Nick: "all uniform in shape and path"): product-distribution sizes (`random_scale2` mult — caught `combineattrscope` defaulting to **'P'**, multiplying random×POSITION, ScaleRand went negative), then **render branch OUTSIDE the SA feedback loop** (`null_pop_out → noise_wobble → noise_breath → null_render_out`, instanceop repointed; solver `Pointsupdatepop` + centroid tracker stay on the clean null — in-loop variation would accumulate into trajectories) for perlin4d wobble + per-particle size breathing; Wobbleamp halved 0.008→0.004 after silhouette scatter. **Audio:** light orbits on integrated energy (`ctrl_orbit_speed → speedCHOP → null_orbit`, period ratios + π offsets preserved); block-arrival bloom pulse (`exp` decay off bar clock, `Blockpulse` 0.6); ember rim strength on sub, curve 1.1. **Lens stack:** depth-driven lumablur DOF — calibrated by **grid-sampling the depth pass** (mass at cameraspace 3.5–4.7, NOT the ~1.9 Camradius implies; unclamped void sampled 2924) — Nick dialed to deep-focus 3px/1.0 over three rounds ("basically everything in focus"); back-third fog (`Fogstart` 0.6 windowed via levelTOP Pre-before-Range ordering); highlight shoulder 0.85; camera micro-drift (incommensurate sins). **Living void bg** (spec gathered by ASKING Nick: eyes-closed darkness + enclosed room presence, "felt but quiet"): macro room-drift + micro grain → SACRAMENT dark-ramp lookup (palette-locked by construction), 3–7% post-grade violet lift. **Aesthetic pattern logged: every atmospheric default got dialed DOWN by Nick (wobble, DOF ×3, fog ×3) — start atmospherics at half.** ctrl_master grew ~25 dials across 7 pages. **td-save run:** v001 saved, 18 moves flushed, 5 new tracker rows (combineattrscope-'P', depth grid-sampling, appendFloat ParGroup, write_dat one-cook TypeError, levelTOP Pre/Range order), no promotions (all count 1). NEXT session: (1) `Barstart` align to track downbeat, (2) full-song held-tension watch (the unverified Act 3 sustain test + emergent-figuration watch), (3) Dreamhaze choreography once Nick supplies the realization timestamp, (4) dormant-op cleanup (lookup chain, `bg_black`, `Basecolor` dials, `Paletteperiodscale`), (5) verify `rec_out` is mjpa+pcm16 (predates today; NC rule). Mirror-flaw idea pitched 3×, not taken — dropped unless Nick raises. |
| 2026-06-12 (skills) | **Brain-system skills built + promotion pipeline verified end-to-end.** Subagent team under skill-creator methodology built three vault skills, installed to `~/.claude/skills/` + registered in `skills/README.md` (v1.1): **`wobar-close-out`** (session ritual encoded — this row is its first live run), **`wobar-promote`** ("promoted = verified landed" — write→grep→mark per entry, born from the 06-10 14-orphan audit; AUDIT mode for back-verification), **`wobar-brand-check`** (act/cusp register check vs WOBAR_EMOTIONAL_REGISTER.md, Vilos cross-brand guard, TWOZERO screenshot capture, scored verdict + CRITICAL/MINOR misses + one concrete move each, advisory-only). **Promote AUDIT maiden run: all 20 PROMOTED tracker marks grep-verified present in AGENT_RULES — zero orphans, zero contradictions; 06-10 repair held.** All cells upgraded to `(verified 2026-06-12)` form. Skill-behavior note: the "try 2–3 phrasings before declaring a miss" rule earned its keep first run (layoutTOP grep hit a section heading only; body confirmed on second look). Brand-check maiden run scored crease_sa 8/10 Act 3 (see companion row). Marginal skill candidates deliberately skipped: ref-network + new-network doc steps (fold into td-save/td-build later), session orientation (hook territory), POPX bump (goes stale), skill-sync (rsync not skill). Also: sa_1 registered in WOBAR_MOVE_SYSTEM mapping table (was fully unregistered — no moves/, no CHANGE_LOG). No loop opened (Nick: log only). |
| 2026-06-10 (stress test) | **Act 4 stress test — full system built in one session, piece KILLED by Nick, heavy brain harvest.** The test: can the brain produce striking+professional Act 4 fast? Built the complete pipeline (procedural obsidian mass w/ Massseed variants → instanced solid chunks → pressure-fuse glowing cracks via interior shadow-casting light → exploded-view discharge → seed-swap reformation w/ hysteresis trigger → THREE switchable envelope drivers: 8-bar clock / audio drop-gate / bar-based choreography table (`anim_song_keys` + `song_kfm`) → integrated tumble + camera breath + post chain). Survived 3 cheap creative pivots (napoleon→GOAT→procedural mass; ember→obsidian; clock→table). **Verdict: system-building strong + fast, aesthetic never landed — figurative/literal-destruction cheese chain (statue→goat→chunk-grid), Nick killed it for a fresh start.** Dead project at `networks/act4_explosion/`, moves in `networks/heave/`. **Harvest: 12 debug-log entries, 12 tracker rows + 1 promotion (isOP sibling paths → AGENT_RULES), reference_networks architecture entry (5 reusable subsystems documented), `working/WOBAR_RELEASE_STANDARD.md` BORN (3-gate release checklist — technical/brand/craft — accretes per session), 2 memories (figurative-cheese lesson, storage diagnosis).** Mid-session: disk-full crisis triaged (Trash + TCC-protected iPhone/iCloud/Spotify caches diagnosed, ~30G freed paths identified); TD build now 2025.32820 (POPX mismatch moot). No loop opened — next session starts the fresh concept clean. |
| 2026-06-10 (later) | **Managerial sweep + loop triage.** (1) **Working tree cleanup:** committed a month of uncommitted prior-session work in 4 chunks — TD network artifacts (magnetize v001–v006 tox, down_bad_3stack toe/moves, mercury_womb, brain_scan_keleidoscope, iris_2 v3), td_library/POPX reference updates, Blender logs, CLAUDE.md cross-brand rule (`fa1e378`…`eec0b58`, pushed). `settings.local.json` untracked from repo (was gitignored but still tracked). (2) **Auto-commit autopsy + FIX:** the 5am vault backup fired exactly once — install day 2026-03-10 — because Obsidian Git only runs while Obsidian is open, and work moved to Claude Code; auto-push was never enabled. **Replaced with launchd agent `com.wobar.autocommit`:** runs `~/Library/Scripts/wobar_vault_autocommit.sh` (canonical copy versioned at `scripts/vault_autocommit.sh`) daily at 5:00 AM — add-all → commit "vault backup: timestamp" if changes → pull --rebase --autostash → push; offline-safe, never leaves a half-rebase. Required granting Full Disk Access to `/bin/zsh` (TCC blocks launchd agents from ~/Desktop — first test failed "Operation not permitted" until Nick granted). Test-fired clean: pull + push verified. Log: `~/Library/Logs/wobar-autocommit.log`. Vault is self-backing again, and now actually pushes. (3) **Release Schedule loop verified after a month stale:** MUR #01 SHIPPED 5/29 on time with the full 7-asset packet — v2 Mirror-only packet works. #02 on track for 6/12. New flag: #03's T-21 was 6/5 (already past) — verify upload or it slides. No BTS trigger fired yet. (4) **Walls Are Thin loop CLOSED** — Nick's call: quiet retirement (option c). Mirror posts going out, no trigger fires, no Transmission signal worth waiting on. BTS-trigger pattern survives as a release-packet mechanism, not a Walls revival path. Moved to `WOBAR_CLOSED.md`. Also this session: caveman plugin installed (JuliusBrussee/caveman, user scope) + global `~/.claude/CLAUDE.md` caveman-mode instruction created (not committable — `~/.claude` not a git repo). |
| 2026-06-10 | **TD system audit — feedback loop repaired, no TD build.** First Fable 5 session; reviewed the whole TD knowledge system (build log, debug log, agent rules, index) for learnings. **Core finding: the promotion pipeline was broken** — 14 Correction Tracker entries marked "✅ PROMOTED" had never actually landed in `WOBAR_TD_AGENT_RULES.md` (only the math.sin and non-commercial-export rules ever made it). All 14 now landed in a new **"Promoted Gotchas — by Operator Family"** section, plus 2 count-2 stragglers promoted (instancetop/instanceop, geometryCOMP torus1) and 1 undercounted recurrence added (audio power curve >1.2 crushes gentle-register modulation — hit 04-16 + 05-23). **Contradictions fixed:** Export section in AGENT_RULES specified h264nvgpu, directly contradicting the non-commercial rule below it (rewritten — mjpa master from TD, transcode outside); the old "13-Parameter Contract" Constant-CHOP architecture rewritten around the actual `ctrl_master` custom-par practice; 3 conflicting feedbackTOP wiring entries reconciled to one canonical pattern (fresh content on input[0], target as par.top, explicit res + resetpulse — the loop warning is NOT cosmetic) in tracker, debug log, and Feedback Chain Rules; stale compositeTOP "verify empirically" entry superseded by the swaporder=True canonical fix. **Audio rules promoted:** record-first/p95 normalization workflow, `Base + React * pow(clamp(audio_n,0,2), Curve)` binding template, max(0,x)**exp wrapping, speedCHOP integration. **Hygiene:** TD_BUILD_LOG split (1189→360 lines; 17 sessions from 2026-05-06 and earlier → new `TD_BUILD_LOG_ARCHIVE.md`); tracker conventions added (dated count bumps, SUPERSEDED markers, "promoted = verified landed"); index metadata refreshed (archive registered, stale File Status dates fixed). Committed `9815ce5`, pushed to origin/main. Self-contained — no loop opened, no existing loop touched. |
| 2026-06-09 | **Glass-orb (magnetize) — finished, brand-checked Act 2 / DESCENSION, recorded, SHIPPED → `magnetize_v006.tox`.** Long session that took the magnetize network from a murmuration rabbit hole to a finished piece via strip-to-legible-base then one intentional decision at a time: reflective mirror-orb → translucent frosted glass → **3 nested shells** → **audio-reactive Lissajous tumble** (bass→outer / mid→mid / high→inner, INTEGRATED via `speedCHOP` so rotation never jumps) → **drop-gated feedback MELT** (energy past `Meltgate` → radial dissolve) → **cool-color rework** (`Saturation` 0.45 + cool-band hue + cool tint = on-brand Act-2 blue/teal; full-rainbow was off-brand). Killed a beat-driven downbeat layer (made it worse → stop signal) and abandoned DOF (transparent glass writes no clean depth — "don't fight the tool"). Network cleaned up (10 dead ops + `Showmarkers` removed, subsystem-band layout, 24 node comments + `description` DAT). `rec_out` set up **NON-COMMERCIAL-safe** (`mjpa` .mov + `pcm16`; h264 is license-gated) → master recorded → HandBrake. **Learnings written to TD_CLAUDE_DEBUG_LOG (5 failures), TD_BUILD_LOG (session entry), reference_networks/README (Glass Orb entry), WOBAR_TD_AGENT_RULES (2 PROMOTED rules: `math.sin()` — recurred from 2026-05-04 = Rule-0 miss; non-commercial no-h264). Memory: non-commercial export constraint.** Brand craft logged: audio-reactivity = the load-bearing Act-2 "breathe with sound" element; **depth = spectrum** ("frequency tunnel") mapping (Nick loved it); cool-hue+desat as on-brand starting point (not hard rule). Held pending proof: drop-melt as Act-2↔Act-4 dial. Loop closed → `WOBAR_CLOSED.md`. |
| 2026-05-26 (later) | **down_bad_3stack v002 — full pipeline wired end-to-end (video timing + audio reactivity + cinematic post + export).** Long working session that took v001's composition all the way to "Nick says it looks really good." (1) **Video timing system:** moviefileinTOP `movie_in` in `specify` playmode with composite index expression supporting Videodelay (intro/freeze) + first forward jump (Videoskipat / Videoskipdur to skip removed verse) + second back-jump (Videojump2at / Videojump2to to loop earlier video to align with the final chorus). 5 dials on new `Video` page on ctrl_master. Currently Videoskipat=82, Videoskipdur=45 (skips verse at video t=1:30-2:03 = 33s; Nick has dialed in his own settings layered on top). Required cookRate retiming 60→30 with time.end retimed 11650→5825 frames to preserve 194.17s duration — heavy comp pipeline couldn't sustain 60 cook-fps which caused me.time.seconds slow-mo. (2) **Audio reactivity infrastructure:** built `base_audio_react` baseCOMP at /project1 using rec_audio's recorded stats (16500 samples × 8 bands across full song). Per-band normalization layer (selectCHOP → mathCHOP preoff=-p10/gain=1/(p_hi-p10) → renameCHOP _n suffix → mergeCHOP → null_out) publishing 8 normalized channels (sub_bass_n, bass_n, mid_n, high_n, energy_n, sub_pressure_n, growl_n, transient_n) mapped p10→0/p90→1 (p95 for sparse transient). Established **per-binding dial pattern:** `Base + React * pow(clamp(audio_n, 0, 2), Curve)` with React (audio amount) + Curve (power shape, 1=linear, 2.5=punchy) exposed per binding. (3) **Three audio bindings wired:** MID `sub_pressure_n → base_psy/level.opacity` (Echoreact/Echocurve), TOP `bass_n → level_posterize.stepsize` (Posterizereact/Posterizecurve), BOT beat-pulse driven CA (see #4). Hierarchy by register: TOP = sustained body weight, MID = atmospheric body swell, BOT = rhythmic punctuation. (4) **Beat pulse system + global CA:** built `exp(-((me.time.seconds * BPM / 60 / div) % 1) * decay)` pulse expression with audio scalar layer (Beatbpm/Beatdiv/Beatdecay/Beataudio dials). Initially tried edge brightness/thicken flash on bottom panel (multiple swaps — none read because binary line art swallows variation), pivoted to **global CA chain** between layout_stack and null_out: 3 levelTOPs (high_g/b=0 for keep_red etc) split RGB → 2 transformTOPs offset R +tx and B -tx by pulse expression → compositeTOP add to recombine → null_out. Beat dials drive Caoffset visible per-beat color split. (5) **Cinematic post chain:** ca_recombine → `level_grade` (Cineblacks/Cinegamma/Cinecontrast) → `hsv_grade` (Cinesat) → `bloom_post` (Bloomint/Bloomthresh/Bloomradius, inputplusbloom) → `grain_apply` (animated simplex2d screen-blended grain, Grainamount). 8 dials on new `Post` page. Grain per IG strategy doubles as banding protection through platform 4:2:0 re-encode. (6) **ctrl_master cleanup** into 4 logical pages (Top/Mid/Bot/Video) + Post, with audio-react dials paired next to parent visual dial on the same page (Echoreact next to Echoopacity, Posterizereact next to Posterize, etc). (7) **Export:** moviefileoutTOP `movie_out` configured per IG specs (h264/yuv420/cabac/Rec.709/18Mbps avg + 24 peak/30fps/AAC 320 stereo/limit 194.17s/uniquesuff to auto-increment file numbers). **Bug found:** initially wired audiochop to `/project1/down_bad_remix_v2_2` (sequential playmode audiofileinCHOP) which plays in real-time from sample 0 regardless of timeline — caused export audio to start from song beginning out of sync with video. Fixed to `/project1/base_audio/audio_in` (locked to timeline) which matches what's heard in live preview. (8) **Brand review against Act 4 Phase 1 register** surfaced 5 minor misses (no forward propulsion, CA may be too aggressive at 8px vs nostalgic 1-3px brand spec, palette not verified, no "discharge" visual moment, no "full palette activated" feel) — Nick deferred all as "not a big deal" and signed off on visual look. **~15 new TD gotchas surfaced** including: moviefileinTOP playmode values are 'locked'/'specify'/'sequential'/'timecodeop' (NOT 'specifyindex'); numFrames attribute renamed to numImages in TD 2025+; moviefileinTOP samplerate par controls index→file-time mapping not playback speed; addSOP requires par.addpts=True BEFORE par.point=N or silently outputs 0 points; sphereSOP uses radx/rady/radz not rad; copySOP semantics input 0=template input 1=source; mathCHOP par.scope only filters which channels get the math (others pass through unchanged) — per-channel normalization needs selectCHOP upstream; mergeCHOP inputs dynamically grow + compact when changes happen, disconnect-via-for-iter leaves stale wires/dupes; compositeTOP type constant is `compositeTOP` not `compTOP`; TD par naming for appendFloat is First-uppercase + rest-lowercase+digits only (`EchoopacityReact` fails, `Echoreact` works); absTime.seconds is wall-clock vs me.time.seconds is timeline (use me.time for timeline-synced refs); cookRate vs timeline.rate vs wall fps interplay causes slow-mo when heavy pipelines can't sustain cookRate; **two audiofileinCHOPs in same project with different playmode is the load-bearing pitfall for moviefileoutTOP audio routing** (always use the locked-to-timeline one for synced export); feedbackTOP cook-loop-safe wiring (par.top = target, input[0] = different op for format/init); noiseTOP GPU types only (simplex2d) — CPU types freeze TD. **6 new reusable patterns** to document in TD_BUILD_LOG: rec_audio-derived per-band CHOP normalization layer publishing _n channels; audio binding via `Base + React * pow(clamp(audio_n, 0, 2), Curve)` with paired dials; tempo-driven beat pulse expression with audio scalar layer; 3-channel CA via 3 levelTOPs + 2 transformTOPs + 1 add compositeTOP; cinematic HDR post template (level → hsv → bloom → grain) with Post-page dial set; moviefileoutTOP audio routing rule (locked-to-timeline source, not sequential). Network unsaved as canonical .toe at session close — risk of losing work, flagged in NEXT. |
| 2026-05-26 | **POPX v1.4.0 brain-system update.** Pulled changelog from popx.tools/changelog#release-v1-4-0 and updated three live brain files. `reference/td_library/TD_POPX_GUIDE.md` bumped 1.1 → 1.2 with a new `## v1.4.0 Changelog Highlights` section near the top covering: **Filter modifier** (new — Gaussian / Left Half Gaussian / Box / Left Half Box / De-Spike / One Euro adaptive — documented as its own section between Spring and Texture Falloff, plus added two new rows to the capability table); **breaking** removal of per-curve toggles on Sweep / Move Along Curve / Orient Curve (per-curve TOP mapping is now automatic — N curves → N rows of TOP); **renames** consolidating ramp UI under Custom Ramp Editor across ~10 modules; **Path Tracer** gains DLSS (Windows/NVIDIA only — M1 stays on SVGF/OptiX), Microfacet Refraction, per-channel OptiX reject thresholds; **POPX Light** gains Diffuse/Specular Contribution; **Move Along Mesh** Distribution menu; collision accuracy on Soft Body / Relax / DLG; **Material `Emssion*` typo FIXED** → `Emission*` (fixed inline at lines 490 + 525). All v1.3.0 example paths in the guide updated to `touchdesigner/third_party/POPX/POPX_Examples_1_4_0/`. Two index files updated: `WOBAR_TD_INDEX.md` POPX pointer + `td_library/TD_LIBRARY_INDEX.md` POPX row both reflect 1.4.0. Historical artifacts (network move files, CHANGE_LOGs, prior session-log entries) intentionally not rewritten — they accurately record state under 1.3.0. New loop opened: POPX v1.4.0 Upgrade. Two open questions for Nick: install confirmation in live TD project, and TD build bump 2025.32460 → 2025.32820. |
| 2026-05-23 (later) | **Vilos subbrain established + cross-brand rule added to BOTH CLAUDE.mds.** Built parallel Vilos workspace at `/Users/nicholasrabow/Desktop/vilos/vilos/` from imported VILOS knowledge vault zip (INDEX.md, REFERENCE.md, ACTIVE.md, World Bible / Visual Brief / Brand Brain PDFs, full branding assets including logo + backgrounds + brand book PDF). Vilos uses different doc structure than Wobar — single REFERENCE.md as operating system (not multi-file WOBAR_BRAND/FRAMEWORK/etc) — adopted that pattern in CLAUDE.md. **Cross-brand rule added to wobar CLAUDE.md (and vilos CLAUDE.md): before starting ANY new visualizer, ASK "Is this for Vilos or Wobar?" unless user gives unambiguous context.** Two brand projects on same machine, completely different rules — Vilos = Melodic House & Techno 126-128 BPM Anyma-lane, palette is black/teal/purple/white only, river-of-light signature, hyperclarity-not-distortion psychedelic; Wobar = psychedelic bass, desat-psychedelic palette, mirrors-and-encounter lens. First Vilos TD session also happened this date — built a filter bank pattern (base_grade + base_magic + base_riverlight, switchTOP routing, ctrl_master with 6 dials). Full vilos session entry in `/Users/nicholasrabow/Desktop/vilos/vilos/ACTIVE.md` + `working/TD_BUILD_LOG.md`. |
| 2026-05-23 | **down_bad_3stack v001 — composition + three psychedelic treatments + ctrl_master built; audio reactivity NOT wired.** Long iterative session. Started from Down Bad music video (Dreamville/J Cole) loaded into TD as YTDown_YouTube_... moviefileinTOP (renamed `movie_in`). Built 720×1280 portrait 3-cell vertical stack via 3 selectTOPs + layoutTOP (vert top-bottom, scaleres=True, fitoutside). Iterated through treatment options (feedback echo, palette lookup, kaleido, drip, ca, etc) — Nick picked FEEDBACK ECHO for first cell, then PALETTE LOOKUP for the middle, then EDGE DETECTION (white on black) for the bottom. Each built as reusable baseCOMP with internal in1/out1 connectors and `../ctrl_master` relative-path bindings (per the no-absolute-paths rule). `ctrl_master` ended with 3 pages × 11 custom params: Top Echo (Echoexpand/Echoopacity/Echorotate/Echodriftxy), Color (Colorphase/Colorcycle/Colormix), Edge (Edgestrength/Edgeblack/EdgecolorRGB). Mid-session swapped top↔middle for stronger rhythm (legible→kinetic→graphic instead of kinetic→legible→graphic). Then Nick saved as `down_bad_3_stack.1.toe` in the workspace folder — relative file paths broke because TD preserves the path string but the relative origin moves to the new .toe location; repointed `movie_in` and audio CHOP to absolute paths to recover. **Brand check (`WOBAR_EMOTIONAL_REGISTER.md` Act 4 Phase 1) flagged critical miss: composition is mirror/encounter-strong but the visual is "still water" — no audio reactivity wired, violates "the body cannot discharge in still water" failure criterion. Also: pure-white edge color violates `WOBAR_BRAND.md` "never pure white"; top cell feedback chain runs on raw saturated source colors instead of pre-desat range.** 7 new TD gotchas surfaced (3 of them recurring footguns that should be promoted): compositeTOP `over` swaporder=False puts INPUT 0 on top (now PROMOTED — was 2026-05-04 entry at count=1, now count=2); layoutTOP inputs auto-compact when disconnect()'d (bit me 4× this session — every layout rewire needs to re-assert all 3 inputs); baseCOMP can't `.connect()` directly to another op's input slot — must pass `.outputConnectors[N]`; lookupTOP par.channel default `independent` gives weak recolor — use `luminance` for strong tonal remap; edgeTOP par.alphaoutputmenu default `edge` makes non-edge areas transparent — set `one` for solid bg; transformTOP rotation is `par.rotate` (single value), not `par.r` (tuplet group); TD Save As preserves relative path STRINGS but the relative origin moves to the new .toe — file paths break silently. Also re-confirmed: `..` in TD parameter expressions walks up to PARENT COMP (so from xform inside base_psy, `../ctrl_master` = `/project1/ctrl_master` — one level, not two). Mercury_womb loop paused (moved to WOBAR_CLOSED.md). |
| 2026-05-06 (late) | **Act 2 Visual — tunnel export loop closed.** Visual was signed off on 2026-04-14; loop has been waiting on a per-release render that's better treated as per-release work, not per-build close-out. Same workflow applies to all shipped networks now (`iris_2`, `eyes_cut_deeper`, `act2_tunnel`) — render is selected from the shipped catalog when a release schedules them, not held open as a network-side blocker. Loop moved to `WOBAR_CLOSED.md`. |
| 2026-05-06 (late) | **Brand docs catch-up — both loops closed.** (1) Rewrote `WOBAR_BRAND.md` Color Palette section to align with the 2026-04-30 desat-psychedelic refresh: replaced the locked 80/60/30/10 percentage-tier framework with the all-first-class vocabulary (foundation = black/off-black-purple/charcoal-mauve, purple spine, everything else first-class — mauves/magentas/cools/oxidized-organics/warm-desat/mirror-metallics/pale-bone), added "no required-or-forbidden per act" + Lookup-TOP-route note, deprecated the percentage-tier framework explicitly. Brand doc + TD docs now consistent. (2) Added CA as PERMITTED to `WOBAR_TD_AGENT_RULES.md` Materials section with explicit "nostalgic / VHS / analog signal distortion — not as rainbow effect" framing; added "Chromatic aberration — nostalgic register, not act-locked" subsection explaining on-brand vs off-brand tone (muted+small=analog, saturated+large=rave), the 7-op recipe, tuning ranges, and the eyes_cut_deeper TR cell as canonical example. Also added new CHROMATIC ABERRATION primitive to `WOBAR_TD_REFERENCE.md §3` Visual Primitives Vocabulary with full chain and params. CA is now a formal non-act-locked primitive in the vocabulary. Both brand-doc loops moved to `WOBAR_CLOSED.md`. |
| 2026-05-06 | **iris_2 visualizer for Eyes Cut Deeper — shipped.** Full-song render committed. Loop closed → `WOBAR_CLOSED.md` with full architecture/learnings preserved. |
| 2026-05-06 | WOBAR_CONTENT.md v2.0 rewrite. Cut Walls Are Thin bucket, format system (Mirror/Room/Transmission), posting system, baseline monitoring, 10-asset gate. New shape: release content only. Original Track package = full track + 1 visualizer per in-song drop + reactive BTS (gut call — could be Nick likes it, could be over-baseline performance, his decision). Series content lives in working docs until promoted. Walls Are Thin no longer in brand docs. Drift to reconcile: Release Schedule loop locks BTS trigger at 1.5× baseline; brand doc loosens to gut call. |
| 2026-05-06 | Release schedule loop opened. Reviewed v1 release packet (uploads/wobar_release_packet.xlsx), diagnosed 200-piece-per-release scope as the bottleneck that stalled MUR (track done since 4/3, system blocked execution). Built v2 packet at /working/RELEASE_PACKET.xlsx — 4 tabs (SCHEDULE, TEMPLATE, MUR, ARCHIVE INDEX). Mirror-only, 7 required assets (5 visualizers + artwork + track), T-counter formulas off Release Date so duplicating the tab shifts the schedule. Biweekly cadence locked: 16 releases through 12/31 (5/29 → 12/25 Fridays). MUR pre-filled as #01. BTS decoupled from required content, now lives as trigger-based companion to Mirror via 1.5× baseline rule (TD network screen capture with song playing, no voiceover). Walls Are Thin decoupled from release packet entirely — opened as separate review loop. TD output target locked at 3/week floor = 96 visualizers produced vs 80 needed = 16 surplus by year-end. MUR T-21 deadline is 2026-05-08. |
| 2026-05-06 | **iris_2 single-eye visualizer for Eyes Cut Deeper — composition + audio reactivity built, NOT yet rendered.** Long iterative session. Started from a Midjourney-generated photoreal iris source (1024×1024 square) and built a complete cusp 4→5 visualizer that outputs at 720×1280 portrait. Composition migrated through 4 iterations: vertical mirror with hardcut → asymmetric mirror → independent counter-rotating halves with 15° tilted cut → **single rotating eye with circular vignette + black pupil overlay anchor (kept)**. Mirror experiments rejected as "too many fragmentation signals competing — broken, not intentionally fractured." Final composition: source iris → audio-driven breath displacement → rotation (clockwise via integrator negation) → palette lookup (phase-animated) → portrait aspect crop (sx=1.78 to preserve circular pupil) → muted hsvAdjust → translucent ghost trails (over-blend feedback) → circular vignette (fitvert for true-circle on portrait) → drawn black pupil with organic edge displacement → cinematic level grade (crushed blacks, S-curve gamma) → HDR bloom (`inputplusbloom`, threshold=0.4, intensity=1.1) → moviefileoutTOP. **Audio reactivity remap:** recorded full song through `audio_master`, computed per-channel statistics (min/p10/p25/p50/p75/p90/p95/p99/max/mean) from rec_audio's 16500 samples × 8 channels. Built per-channel CHOP normalization layer (floors subtract + scales multiply + clamp 0-1) so every channel uses the full 0-1 range based on observed p99. Final mappings: rotation ← `bass^Rotcurve` (sustained) + `transient^Rotcurve` (snap), color cycle ← `energy^2` (slow-on-break, fast-on-drop, ~20× dynamic range — pivoted from growl which never quiets), breath amp ← `bass^Breathcurve`, pupil edge displacement ← `energy^3` (organic during drops only, clean circle during breaks). **Pupil keyframed not audio-reactive** per Nick's call ("more cinematic, more controllable"): table-driven keyframe system (`anim_pupil_keys` tableDAT with time/radius/curve columns + `pupil_kfm` textDAT module with linear/smooth/ease-in/ease-out interpolation). Sine-wave pupil breath stays through all transitions for always-alive heartbeat. **Pin-point pupil at drops** chosen over dilation (counter-intuitive vs typical EDM but reveals MORE iris during peak chaos — pre-drop dilation amplifies the snap). **Brand check landed at 8/10 cusp 4→5:** strong on heavy (black pupil, crushed darks, deep palette) + beautiful (HDR bloom, luminous fibers); drifting on color phase (cycle weights all WOBAR colors equally, could re-weight to dwell in heavy register), peak rotation borderline Act 4 manic, pupil keyframes still placeholder timestamps. **16 new TD gotchas** added to correction tracker including: mathCHOP `chopop` vs `chanop` silent failure (PROMOTED), cross-COMP CHOP wiring fails silently / use selectCHOP bridge (PROMOTED), `null_audio` 60Hz analysis blocks moviefileoutTOP audio (PROMOTED), nested function defs in td_execute_python fail same as list comps (PROMOTED), tableDAT cell backticks don't auto-evaluate (need parameterexecuteDAT), circleTOP `bgalpha` not `bgcolora`, `audiofileinCHOP.par.indexunit='seconds'` default, rampTOP `fitaspect='fitvert'` for circular on portrait, TD non-realtime export mode + audio clicks (PROMOTED). **6 new reusable patterns** documented in TD_BUILD_LOG: HDR cinematic eye chain, phase-animated palette cycle, per-channel CHOP normalization layer, table-driven keyframe choreography, single-eye-with-pupil-overlay composition, curve-gated displacement. Network at `touchdesigner/networks/iris_2/iris.toe`. Final ctrl_master: 8 pages × 30 params (Source / Rotation / Color / Pupil / Breath / Vignette / Feedback). 38 ops total, organized left-to-right. Recorder configured (h264 yuv420 mp4, audio at 44100Hz from audio_in). Open: render is pending, pupil keyframes need actual song timestamps, brand-doc loops still open from prior sessions. |
| 2026-05-05 (late) | **Visual identity review — built `reference/WOBAR_EMOTIONAL_REGISTER.md` (locked v1.0).** New canonical doc for the on-brand check during creative sessions: 5 acts + 4 cusps, each with felt sense / posture / implicit ask / quality of movement / what breaks it / on-brand check. Purely emotional — no palettes, no ops, no build recipes. The brand-check happens here; implementation lives in TD docs. Nick's framing: "I want this to be the sounding board so I can ask 'is this on brand?' mid-session and the check is whether the visual emotionally aligns with the act this track lives in, rather than worrying about implementation details." Built act-by-act through Q&A — felt sense, posture, implicit ask, what breaks it. The key implicit asks landed: Act 1 = "you are safe here", Act 2 = "follow me", Act 3 = "sit with this", Act 4 = "move", Act 5 = "feel". Cusps captured the slow-arc territory where most tracks live: 1→2 ("remember you are safe to let go"), 2→3 (anticipation, distinct marking moment), 3→4 ("settle into it"), 4→5 ("heavy and beautiful" — heart-space opening, where Eyes Cut Deeper lives). Iris-portal loop closed unfinished (didn't work how Nick wanted — chromakey blink masking failed, pivoted to grid). Eyes Cut Deeper grid loop closed — full song rendered, album snapshot saved. Two brand-doc loops (palette section in WOBAR_BRAND.md, CA/nostalgic in WOBAR_TD_AGENT_RULES.md) still open — emotional register doc didn't address them, they're separate doc-rewrite tasks. Suggested follow-ups (logged but not done): cross-reference the new emotional register doc from WOBAR_BRAND.md, WOBAR_FRAMEWORK.md, WOBAR_TD_AGENT_RULES.md so future sessions land on it as the brand-check authority. |
| 2026-05-05 | **Eyes Cut Deeper 4-cell grid — music video + album cover branch built, NOT yet rendered.** Long working session, started from prior day's grid composite (4 macro-eye videos in 2×2). Major arc: established per-cell distinct effects (kaleidoscope mandala / CA+TV static / zoom-tunnel / oily ripple), each with WOBAR-palette grade lookup; per-band audio reactivity (TL=energy, TR=transient, BL=sub_pressure, BR=growl) instead of single broadband Energy; canonical `base_audio_v001.tox` swap (got 8 channels with band_max normalization vs. our previous 5-channel custom build); per-band gain controls on ctrl_master; wobble inverse-to-energy on mandala (perfect mirror at drops, moves at breaks); 4-fold radial kaleido (rotations not bilateral) for ceremonial register; TV static using `randomgpu` recentered via offset for proper ADD blend (not overlay); final polish chain (simplex2d film grain + radial vignette) unifying disparate cell palettes; black inter-cell gaps (sx=0.485, sy=0.4925 for portrait/square balance); parallel `null_album_out` branch at 720×720 with aspect-fill center-crop; `rec_out` moviefileoutTOP for video, `album_snapshot` for stills, one TIFF saved. **Notable insights:** new song mur.wav vs eyes_cut_deeper has different band profile (growl-dominated, not sub-sustained — BL tunnel needs gain 50 to compensate); `audio_in[0].sample()` returns near-zero values that are bipolar rms (use rmspower not rms for proper RMS); mandala explosion needs `outhigh` on level (RGB-scale) not `opacity` (alpha-only) to fade out under max-blend. **5 new TD gotchas surfaced:** `randomgpu` outputs centered at 0.5 — kills `overlay` blend (use offset to recenter ±range or switch to ADD); `mathCHOP.chanop=average` keeps channels separate (set `chanop=off` and `chopop` for cross-channel); `selectCHOP.par.channames='chan1'` selects first channel by name (not index); `levelTOP.outhigh` modulates RGB scale, `opacity` only modulates alpha — for max-blend fade-out use outhigh; `feedbackTOP` cook-loop warning is REAL (not cosmetic) — wire input[0] to fresh content (upstream of loop), use par.top for the loop reference, pulse resetpulse if resolution gets stuck at 128×128. **2 new loops opened:** WOBAR_BRAND.md palette section update (drop 60/30/10 tier framework, align with desat-psychedelic refresh from 2026-04-30), and CA/nostalgic-register doc update in WOBAR_TD_AGENT_RULES.md Materials. Network not yet shipped — full-song render pending. |
| 2026-05-04 | **iris network — 13 moves, NOT shipped.** Started from a "TD Drop #05 Eyes" YouTube tutorial base for the Subtronics "Eyes Cut Deeper" remix (Act 4/5 cusp, 95 BPM halftime). Twisted-torus geometry where the inner cylinder doubles as the tunnel-when-camera-dives-through-pupil — clever single-geo solution to the "iris becomes tunnel" mechanic. Composition arc through the session: recolor to monochrome purple (limbal+stroma+ruff+amber-fleck+slate+bone-ash palette via `ramp2_keys`) → dominant scale (`transform2.scale=2.2`) → light recenter+brighten+cool-violet tint → pupil baseline (rady 0.65→0.5, opens hole, hints at inner tunnel) → halo pulse (10s breath via expression on `ramp3.period`) → black pupil mask (`circle_pupil` 175px) → feedback boundary mask (`circle_iris_bound` × `comp_iris_bound` between comp4 and downstream — kills the trail-overflow tail) → ctrl_master baseCOMP with `Puprad`, `Irisx`, `Irisy`, `Irisscale` exposed. **Failed experiments (reverted):** warm halo color tint (added defined ring shape that competed with iris), catchlight in pupil (premultrgbbyalpha gotcha; even after fixing, position kept overlapping inner-cylinder glint), wet PBR (light dimmer at 5.5 blew out any glossy surface to plastic-mirror), limbal ring overlay (merged with natural alpha falloff, barely visible). **Iris-over-video branch:** built parallel composite (xform + mask + comp_video_replace + null_video_replace_out) for replacing the natural iris in a 464×832 macro-eye blinking video with our WOBAR iris. Position/scale tunable live via ctrl_master.Replace. Chromakey-based blink masking failed — natural iris HSV overlaps with sclera and lid skin in this footage; can't cleanly separate iris from "lid-covering-iris" via color. Reverted chromakey ops. **Major TD gotchas surfaced (5 new):** compositeTOP `over` with swaporder=False puts input 0 ON TOP empirically (contradicts entry 69's convention — needs verification per use); circleTOP `centerunit=fraction` makes centerx/y an OFFSET from justify-anchor, not absolute (default 0,0 = center); circleTOP `radiusunit=fraction` is anisotropic on non-square frames (use fractionaspect or pixels); circleTOP `premultrgbbyalpha=True` (default) can hide small/dim circles in composites — set False; TD param expressions: bare `sin()` not in namespace, must use `math.sin()`. Other learnings: backface culling on twisted geometry doesn't reliably remove visible "back" surfaces because twist makes orientation ambiguous; wet PBR requires concurrent light dimmer rebalance. Network not finished — open loop. |
| 2026-05-01 (late) | **magnet_chamber v002 shipped — inside-the-tesseract psychedelic variant**. Same magnetize core as v001 but pivoted to Cooper-in-4D / wireframe-cubes-from-inside register. Major architectural learning: `wireframeSOP → convertSOP polygonize → instance` is REQUIRED — wireframe alone breaks geometryCOMP instancing (only 1 instance renders without convert). Manual chromatic aberration via 3-channel levelTOP isolation + 2 transformTOPs (TD has no native CA op). Inside-POV camera (Camradius=0.1, Camfov=80) for immersive perspective. Per-cube random Rotate/ScaleRand attrs via randomPOP chain (sphere → cube → tilted cube hierarchy). Smooth sin scale-pulse warp (cubes grow/shrink past camera, 12s period — chose smooth sin over sawtooth because snap-back was visibly jarring). 4-fold mirror compositing. Slow camera tumble (cam.rz/rx). New "Effects" page on `ctrl_master` with 9 params (Caoffset, Tumble enable/speed, Pulse amp/period/baseline, Hue enable/rate, Wireradius). Total ctrl_master now 30 params across 7 pages. Move file: `touchdesigner/networks/magnet_chamber/moves/move_002.json`. CHANGE_LOG.md updated with full v002 spec. **Six new TD gotchas** to log: wireframe-needs-polygonize, manual-CA-no-native-op, feedbackTOP-both-par.top-and-input-required, hue-rotation-needs-saturation, sawtooth-snap-visible-prefer-sin, inside-POV-needs-min-clamp-lifted. |
| 2026-05-01 | **magnet_chamber v001 shipped** — second POPX-on-WOBAR primitive after attractor_chamber. POPX `magnetize` (Mode='spin') with 2 orbiting attractor poles, 2200 native pointgenerator particles, sphere instancing via the proven SA-pattern, HDR env + 2 palette-cycling lights (warm/cool dyad), bloom branch, **4-fold mirror compositing** (h flip + v flip = mandala/sigil register), camera orbit 60s, ctrl_master baseCOMP with 21 params across 6 pages (Audio/Magnets/Material/Lighting/Camera/Composition), recorder wired. Single audio binding: `mag.Strength = floor + ceil*max(0,energy)^exp` — same pattern as attractor_chamber. Methodological win that unblocked this build: **POPX attribute-screening method validated** — place a POPX module, list `output.pointAttributes`, only commit if attrs are 'clean' (P + transform-related only) without solver-internal names (PartId, PartForce, Density, Parent, iNumChildren). Magnetize passed; SA passed; DLA/Particle/DLG failed earlier today. Move file: `touchdesigner/networks/magnet_chamber/moves/move_001.json`. CHANGE_LOG.md created. WOBAR_MOVE_SYSTEM.md mapping updated. Two POPX primitives now confirmed shipping-grade. |
| 2026-05-01 | POPX exploration day — same calendar day as the morning's attractor_chamber close-out; afternoon pivoted to "what's the next POPX visual?" Goal: find a second WOBAR-on-brand POPX primitive complementary to SA. Walked through DLA (radial tree), physarum (lattice network), POPX Particle SSFR (liquid mirror), DLG (brain-coral labyrinth) — **none shipped**. Each ran into the same TD plumbing wall: POPX modules with rich POP attributes (PartVel/PartId/PartForce/Color etc.) cannot be instanced via `geometryCOMP` — both `oplength` mode and `manual` mode reject these POPs with "Manual number of instances not supported when using a POP with point co[lor]". Workaround attempts (attributePOP to strip Color, popToSOP, scriptTOP rasterize) either failed the same way or crashed TD (scriptTOP at `CookLevel.ALWAYS` locked the MCP server twice). What WORKS: physarum's trail TOP, native particlePOP with manual `numPoints()` count, POPX SA's simple-attr output. Three new TD gotchas promoted to correction tracker (geo-COMP-rejects-rich-POPs ×3, oplength-reads-1 ×2, scriptTOP-ALWAYS-crashes ×2). No move file written. **Pattern discovered: TOP-output POPX modules (Flow, physarum, Voxelize) bypass the wall entirely** — that's the path forward when next session resumes. Alternate: GLSL TOP shaders skip POP plumbing entirely. Full session entry in `working/TD_BUILD_LOG.md` 2026-05-01 (POPX exploration day). |
| 2026-05-01 | **attractor_chamber v002 shipped** — first polished POPX-on-WOBAR proof. Built directly at `/project1` (not wrapped in baseCOMP). Architecture: SA Aizawa point cloud (3000 pts, advect mode, audio-bound Timescale) → math_velocity → random_scale (deterministic per-particle ScaleRand 0.6-1.4, decoupled from velocity) → null_pop_out → instanced black-pearl PBR spheres → mirror-symmetric composite → atmospheric post → recorder. 4 lights orbit/cycle through palette channels (warm/cool/earth/pale, non-syncing primes). Centroid tracker (executeDAT + EMA) keeps lookat target locked to live mass-center. **Audio reactivity limited to ONE binding per Nick:** `sa1.Timescale = floor + ceil*max(0,energy)^exp` — speed responds to energy, nothing else. Built `ctrl_master` baseCOMP with 22 custom params across 6 pages (Audio / Material / Lighting / Camera / Composition / Form) — every visual op param reads from this dashboard. Two register tunings landed: hard chaotic mushroom trip (toxic palette, fast breath, exp 1.3) and dark hypnotic crimson (red-only palette, slow heartbeat, exp 1.7). Pink killed at user request. Recorder wired (HAP video + audio_in stereo). 27 orphan ops cleaned up. Six new TD gotchas → `TD_BUILD_LOG.md`. Move file: `touchdesigner/networks/attractor_chamber/moves/move_002.json`. Loop closed → `WOBAR_CLOSED.md`. |
| 2026-04-30 | Session close-out — clean slate. Closed 9 loops to WOBAR_CLOSED.md, leaving only "First POPX-on-WOBAR Build" open. Closed: Act 1 Glass Orb + Motes (paused mid-tuning, network preserved), Act 1 base_act1_underwater (signed off, render not committed), Ferrofluid Sphere (signed off, render not committed), Act 2 tunnel export (signed off, full-song export pending), Act 3 base_pop_sphere (signed off, render not committed), Format Testing The Cut / Stacked / Transmission, Carousel Format Definition. All visual loops were built under the **old** visual identity — they are re-evaluable against the new palette / materials / no-act-rules regime if Nick chooses to revisit before render commit. The new POPX build is the cleanest forward path: it'll be the **first network built under the new identity** as well as the first proof of the POPX guide. |
| 2026-04-30 | Visual identity refresh + TD docs update. Pulled current visual identity baseline from existing brand/TD docs, walked through three changes from Nick: (1) visual identity lens shifts to **mirrors and encounter** over portals and journey — brand docs left locked, lens applies to visual decisions only, (2) **metallic / glossy / reflective surfaces now permitted** — gloss is no longer banned; LED-style glow is OK if it stays desaturated; lean from traditional rave brightness, (3) **color palette expanded to the full desaturated psychedelic range** — black + deep purple stay as foundation, but mauves, magentas, slates, oxidized organics (sage / moss / patina), warm desaturateds (amber / rust / tobacco / brass / coral), mirror metallics (tarnished silver / oxidized copper / bronze patina / pewter), and bone/ash highlights are all first-class — no more rare/accent tier. Palette sketched as ~30 named hex values; Nick approved. **Killed the per-act required/forbidden constraint table** — acts now have emotional registers + shared visual vocabulary, no enforceable rules. Updates landed in: `WOBAR_TD_AGENT_RULES.md` (v3.0 — new Visual Identity Lens, Color System rewritten, new Materials and Surface section, act constraint table deleted), `WOBAR_TD_REFERENCE.md` (§3 renamed to "Visual Primitives — Vocabulary" with affinities not act-locks; §4 fully replaced with the new swatch + render-in-palette vs Lookup-route pipeline + per-act affinity suggestions; §6 archive footage chain renamed Mirror-Frame), `WOBAR_GLSL_PATTERNS.md` (v2.0 — Constraints preambles softened to Sensibility, vec3 palette block of 32 named colors replaces forbidden table), `WOBAR_TD_INDEX.md` (pointer descriptions updated), `.claude/commands/td-build.md` (Step 5 from hard validate → optional sense-check). Brand docs (`WOBAR_BRAND.md`, `WOBAR_FRAMEWORK.md`, `WOBAR_COPY.md`) untouched per Nick's call. |
| 2026-04-30 | POPX library survey complete. Walked alphabetically through all 55 official examples in `touchdesigner/POPX_Examples_1_3_0/examples/` (`aim.toe` → `voxelize.toe`). Each example: load → fix `parent.Loader` cascade errors (Reset / Resolutiony / fileinPOP.par.file all reference a missing parent COMP) → screenshot → param inspection → document new findings into `reference/td_library/TD_POPX_GUIDE.md`. Guide promoted from version 0.1 → 1.0 (status: complete). Final state: 2651 lines, 65 capability-table mappings, ~30 modules with full param pages and canonical patterns, cumulative integration patterns section near end. New patterns confirmed: SA chaos-attractor cloud (Lorenz/Thomas/Aizawa), Subdivider crystalline spikes with falloff masking, Spring on attribute (not transform) for elastic per-point lag, Sweep per-curve TOP modulation (image/spectrogram/video → 3D stacked sweeps), Sweep custom cross-section via `Surfaceshape='input'`, Soft Body suite (cloth/struts/pressure/string), strings/hair-on-rigged-character recipe via `Pintype='stopped'+Matchanimation=True`, typography per-letter rig (Convert+Pivot+Spring+TransformModifier+Unpack), living Voronoi via Relax-feedback, voxel-art via Voxelize.output[3] + `geometryCOMP.par.instanceactive='Inside'`. Glass Orb + Motes loop paused throughout this work — to be resumed. New loop opened: "First POPX-on-WOBAR Build" — pick one canonical pattern, validate the guide produces real value before declaring victory. |
| 2026-04-27 | Glass Orb mote system — long iterative build inside `/project1/base_glass_orb/base_motes`. Started with 3 motes refracted through the existing glass orb shader (input[2] sampled at refracted UVs). Built up: per-body color + breath pulse, depth dimming, 3-stage screen-blended Gaussian bloom for "ball of light" feel, perspective camera with parallax + center alignment, hard cage with velocity reflection, audio reactivity (sub_bass→core brightness, energy²→gravity). Many iterations on size/brightness/color — settled on warm Act 1 trio (gold/magenta/purple) at moderate brightness with screen-blend bloom that preserves hue. Removed orb specular highlights. Added film grain layer. Tried + rolled back: trailing tails (blown out), audio-triggered kicks (too aggressive), 5 motes (too frantic), Lennard-Jones inter-body repulsion (wrong solution). Latest fix: SPREAD-based thermostat replacing velocity-based (which failed for "spinning tight ball" cluster failure mode). Many new TD patterns logged to debug log + correction tracker (geometryCOMP auto-torus footgun, compositeTOP 2-input limit, me.storage scope in scriptCHOP onCook, pow() with negative-noise floats, scriptCHOP per-frame cook requires time-dependent input, Fibonacci pole zero-velocity bug). Move files 005-013 in glass_orb/moves/. |
| 2026-04-21 | base_act1_underwater build session (continued from prior context). Fixed particle instancing (Script CHOP callbacks must be edited in-place via text.replace — TD regenerates template if you overwrite whole DAT). Resolved camera drift to tz=-94 (absTime.seconds → me.time.seconds). Bypassed POP→CHOP download pipeline (6.7fps) — static Script CHOP positions at 0.4% budget. Boosted caustic brightness (power 2.2→1.3, base mult 0.65→1.0). Reduced sphere radius 0.025→0.008 for fine sediment scale. Visual in progress, pending Nick sign-off. Key patterns: Script CHOP callback edit path; me.time.seconds for camera; poptoCHOP not viable at 1800pts 60fps; camera-through-cloud = apparent drift. |
| 2026-04-21 | Ferrofluid sphere build session. GPU POP translatealongnormal for radial spikes on black PBR sphere. 3-light iridescent rig (120° hue offset). Pushed iridescent → oil-slick → HDRI environment + backdrop plane. Bioluminescent glow ring on backdrop (centered point light between sphere and backdrop). Feedback trail + pulsing glow added. Network organized, movie_out wired (video-only — AAC blocked on NC). Key patterns: Geometry COMP SOPs must be children inside COMP (no external connectors); op.display=True not setDisplayFlag(); blurTOP CPU-lock at large size; AAC blocked on NC requires FFmpeg mux post-render. |
| 2026-04-18 | base_pop_sphere creative session. Started as psychedelic experiment, evolved into Act 3 "system status indicator" aesthetic — per-sphere green/amber/red color cycling driven by energy (imperceptible at breakdown, chaotic at drop). Radial audio waveform ring added with matching color scheme (abs displacement fix for X/Y drift, rgba8fixed pixel format fix for color). movie_out wired to null_out_final with audio routed from audio_in. mur.wav = 211s = 12,670 frames at 60fps. Visual signed off, export pending.
| 2026-04-17 | base_act2_particles continued then deleted. Tunnel camera, dual concentric rings, brand compliance review + correction. Psychedelic batch tried + reverted. Creative debrief: execution strong, undirected Claude defaults to generic. Network wiped — learning session only, not a keeper. base_audio preserved.
| 2026-04-16 | base_act2_particles built from scratch — Act 2 Descension Vortex. 720-sphere torus overhead view, feedback spiral rotating with bass, Act 2 teal palette via lookup ramp. Key discovery: POP instancing (instancepop/instanceop) broken in TD 2025 — Script CHOP bridge (SOP points → tx/ty/tz CHOP channels) is the only working instancing path. feedbackTOP requires black seed input, not direct wire to target. MAT expressions inside child COMPs require absolute op() paths. Network organized. Build log + reference_networks updated.
| 2026-04-16 | TD general knowledge library built. New brand-agnostic `reference/td_library/` subfolder with 27 files: LIBRARY_INDEX, APPLE_SILICON, NETWORK_VS_GLSL, EFFICIENT_NETWORKS, FOOTGUNS, full operator catalogs (POP/TOP/CHOP/SOP/MAT/DAT/COMP — "every single operator"), pattern library (AUDIO_REACTIVITY, FEEDBACK, GENERATIVE, 3D_SCENES, INSTANCING, COMPOSITING, PARTICLES, TEXT), workflow playbooks (OPTIMIZATION, EXPORT, LIVE_VJ, LIVE_AUDIOREACTIVE, MIDI_OSC, AV_INTEGRATION, PROJECTION_MAPPING, INSTALLATION). Scoped to 2025.32460 Non-Commercial on M1. Explicit POP-first, network-over-GLSL bias per Nick's stated gap. WOBAR_TD_INDEX.md updated with cross-reference. Brand files untouched. |
| 2026-04-16 | base_act2_underwater audio tuning + kick response session. Analyzed rec_audio waveform (16400 samples) — sub_bass peaks 0.81, energy 0.87, growl clipping at 1.02. Fixed: growl_max raised 0.16→0.22, power curves dropped (1.8/2.5 → linear) for Act 1/5 gentle content. Added kick-driven blur (blur_shimmer 0→16px), kick glow burst composite (blur_kick+lvc_kick+comp_kick Add layer). Fixed portal drift — anchored triangle formation, was bottom-left. Added movie_out for recording. Moves 037–043.
| 2026-04-15 | act2_fractal refinement session. Kaleidoscope tunnel: added domain warp (double fbm), global clockwise rotation, color cycling (3 independent oscillators, purple family only), breathing scale, chaos power curve 3.0, sub_pressure power 2.5 for smooth breakdowns. Tried + rejected: mirror cascade, orange palette, full-spectrum psychedelic colors. rec_out wired (h264nvgpu, audio_in at 44100). No checkpoint yet.
| 2026-04-15 | Posting quality research — four-phase investigation into IG/TikTok compression + TD export limitations. Phase 1 platform specs mapped (4:2:0 forced, 15–25 Mbps IG / 10–12 TikTok, desktop web beats mobile on both). Phase 2 pipeline comparison (A retired, C image-sequence→FFmpeg as default on Free license, B reserved for future license upgrade). Phase 3 locked recipe into WOBAR_TD_REFERENCE.md Section 8 — per-platform tables, cross-platform safe zone (center 980×1230), FFmpeg command with Rec.709 + `-tune grain`, upload protocol, quality mitigations. Phase 4 benchmark protocol designed (5-section torture clip) but deferred — battle-testing through live posting instead. Research loop closed. |
| 2026-04-14 | Tunnel audio reactivity session. Built base_audio from scratch (4-band audiofilterCHOP pipeline, energy envelope, kick branch). Built ctrl_audio_live pre-compute CHOP layer in tunnel. Wired all visual parameters to energy: zoom, rotation, opacity, contrast, glow, CA, displace. Intensity parameter on ctrl_master is manual energy ceiling. Explored kick mapping (zoom snap, shockwave, contrast flash — all removed, none felt right). Color cycling via ramp phase tried and removed. Energy-driven Intensity is the keeper — breakdown goes near-still, drop comes alive. |
| 2026-04-12 | Act 2 TD build session continued. Evolved 3-arm spiral from purple to underwater aesthetic. Shifted color to desaturated blue/teal (0.50–0.67 hue, sat 0.40 at shader, 0.75 HSV). Widened wave crest spacing (spacing 0.042→0.072). Added caustic layer (Voronoi edge detection GLSL, 3 animated scales, screen-blended — null_caustic_out). Added radial surface glow (ramp_surface screen-blended — null_surface_out). Brand reviewed: Act 2 DESCENSION alignment strong. Blue color approved as contextual departure. |
| 2026-04-10 | Content format review session. Reviewed existing buckets and format system. Defined three new test formats in working/FORMAT_TESTING.md: The Cut (found footage hook → visualizer at drop, three hook categories: skate/nature chaos/extreme sports), The Stack (three horizontal strips stacked vertical, "you are here" multi-angle), continued Transmission testing. Carousel format parked as research loop. Satisfying process, flow arts, optical illusions killed with reasoning. Formats stay in testing doc until performance data earns them a spot in WOBAR_CONTENT.md. |
| 2026-04-10 | Full loop review. All 8 open loops closed: Common Enemy remix, ASAP Rocky remix, Flow State residency, Thursday event concept, Origin Story series, FREQUENCY Lake Effect, platform bio rewrite, Organic Distortion visual system. Clean slate. |
| 2026-03-30 | Content strategy audit against FSC research. Identified gaps: no baseline monitoring, no format system. Built three wobar-specific formats: The Mirror, The Room, The Transmission. Validated against brand. Baseline monitoring to be added to posting system. |
| 2026-03-15 | FREQUENCY card design locked. Moo Luxe, black stock, soft-touch matte. Raised spot gloss on act names + hidden WOBAR wordmark over logo. White logo, brand purple act names, Futura PT Bold/Heavy. QR centered bottom. One side only. No visible wordmark — IYKYK. Color palette and typography added to WOBAR_BRAND.md from Art Direction Playbook. |
| 2026-03-12 | Common Enemy (Buffalo Farm) remix loop opened. Heavy track. Guitar riff written in C major — working on translation to G minor. Scale degree map and harmonic minor options discussed. Decision point: B as chromatic pull vs E as color note determines approach. |
| 2026-03-12 | Origin Story Content Series. 6-chapter arc built, filmed, captions written and approved. Chapter 2 cut. Chapter 6 folded into The Search. Format locked — video Reels on both TikTok and Instagram. Deployment starts Mar 11. |
| 2026-03-11 | Phone session. Platform bio rewrite loop opened. Character limits confirmed. No copy produced. |
| 2026-03-10 | Origin Story Content Series loop opened. 7-chapter story arc built. TikTok hooks locked. Instagram captions written and brand checked. Two-week deployment calendar mapped. Task list complete. Middle/bottom funnel layer deferred. |
| 2026-03-10 | FREQUENCY event planning session. Lake Effect venue confirmed. Full checklist built and checked against FSC brief and Wobar brand docs. Event agnostic business card concept locked (framework + logo + QR). Landing page scope defined. Content map drafted. Artist outreach — Nick has names, will reach out directly. |
| 2026-03-10 | Audit + systems session. Broken links fixed. WOBAR_CLOSED.md created. Folder structure finalized (root/working/reference). Obsidian Git plugin installed — replaces scheduled task. GitHub connector wired to Claude.ai Project. WOBAR_PROJECT_INSTRUCTIONS.md written. Phone workflow established. No project loops worked. |
| 2026-03-10 | Vault infrastructure session. Git initialized, pushed to GitHub (RabowNicholas/wobar). Daily auto-commit at 5am. WOBAR_ACTIVE, WOBAR_CLOSED created. Folder structure reorganized. No project loops worked. |
